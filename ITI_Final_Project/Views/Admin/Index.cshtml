
@{
    ViewBag.title = "Index";
    int? x = Context.Session.GetInt32("UserId");
    Hotel_App db = new Hotel_App();
    bool? isadmin = db.Customers.Where(m => m.IsAdmin == true).Any();
}
@model List<Cleaner>
<h1>Index</h1>


@{
    if (isadmin==true)
    {


    if (!Model.Any())
    {
        <div class="container mt-4">
            <a class="alert alert-info text-black " style="font-size:20px">No Data</a>
        </div>
    }
                else
                {
                    <div class="col-md-10">
                        <div>
                            <table class="table  table-dark table-sm w-75  text-center p-4 table-hover">
                                <thead>
                                    <tr>
                                        <td>
                                            Name
                                        </td>
                                        <td>
                                            Salary
                                        </td>
                                        <td>
                                            Address
                                        </td>
                                        <td>
                                            Phone Number
                                        </td>  
                                        <td>
                                           Opration
                                        </td>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {

                                        <tr>
                                            <td>@item.Name</td>
                                            <td>@item.Salary</td>
                                            <td>@item.Address</td>
                                            <td>@item.Phone_Number</td>
                                            <td>
                                                <a class="btn btn-primary text-white fs-4 btn-sm" asp-controller="Admin" asp-action="delete" asp-route-Id="@item.Id">Delete</a>
                                                <a class="btn btn-success text-white fs-4 btn-sm" asp-controller="Admin" asp-action="Update" asp-route-Id="@item.Id">Update</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                }
  }
}

